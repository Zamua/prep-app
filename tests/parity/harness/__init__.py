"""The pixel harness.

`compare` is the comparator, `registry` the flow table, `contextspec`
and `capture` the browser side, `server` the local target, `runner`
the golden/compare loop the `test_flows_*` files call into.
"""
