# System-level dependencies for developing prep on macOS.
# Run `brew bundle` in this directory.
#
# mise reads .tool-versions and provisions python / go / bun / goreman
# / temporal-cli at the pinned versions — that's the entire toolchain.
# Linux contributors: skip the Brewfile, install mise via curl
# (see CONTRIBUTING.md).

brew "mise"
