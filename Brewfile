# System-level dependencies for developing prep on macOS.
# Run `brew bundle` in this directory.
#
# mise reads .tool-versions and provisions node / python / bun at the
# pinned versions: that is the whole toolchain. Linux contributors skip
# the Brewfile and install mise via curl (see CONTRIBUTING.md).

brew "mise"
