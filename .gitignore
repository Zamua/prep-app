# Python
.venv/
__pycache__/
*.pyc
*.pyo

# SQLite — user data lives in the DB; restore by re-generating
data.sqlite
data.sqlite-shm
data.sqlite-wal

# Temporal devserver state — local dev only
temporal-data/

# Go build artifacts
worker-go/bin/
*.exe

# UI sweep tooling — local-only, regenerable
ui-tools/.venv/
ui-screenshots/

# CodeMirror bundle build node_modules — we ship the built cm-bundle.js
# but not the node_modules used to build it.
static/cm/node_modules/

# Editor / OS noise
.DS_Store
.idea/
.vscode/
*.swp
